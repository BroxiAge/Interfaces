var cv, cx;
window.onload = function() {
    
    objetos = [];
    
    cv = document.getElementById('canvas');
    cx = cv.getContext('2d');

    objetos.push({
        x:0, y:0,
        wid
    })
};